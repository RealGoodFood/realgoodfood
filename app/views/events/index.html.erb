			<%= gmaps4rails(@json) %>


<div id="tabs">
	<ul>
		<li><a href="#tabs-1">List view</a></li>
		<li><a href="#tabs-2">Map view</a></li>
		<li><a href="events/new"> Create New Even  </a></li>
	</ul>
	<div id="tabs-1">
		<table>
    		        <tr>
      
			        <th>Name:</th>
			      	<th>Description</th>
				<th>Event starting on:</th>
				<th>Event ending on:</th>	
				
    			</tr>
			
			<tr>
			<% @events.each do |book| %></td>
    				<td><%= book.name %></td>
			        <td><%= book.event_description %></td>
				<td><%= book.event_starting_on %></td>
				<td><%= book.event_ending_on %></td>
	
				 <td><%= link_to 'Show', book %></td>
    				<td><%= link_to 'Edit', edit_event_path(book) %></td>
    				<td><%= link_to 'Destroy', book, :confirm => 'Are you sure?', :method => :delete %></td>
  
      			</tr>

			<% end %>
		</table>
	</div>

	<div id="tabs-2">

			<%= gmaps4rails(@json) %>

	</div>
	
</div>
		


