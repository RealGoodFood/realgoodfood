<%= form_for(@member_attending_event_register) do |f| %>
  <% if @member_attending_event_register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_attending_event_register.errors.count, "error") %> prohibited this member_attending_event_register from being saved:</h2>

      <ul>
      <% @member_attending_event_register.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.text_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :state %><br />
    <%= f.text_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
